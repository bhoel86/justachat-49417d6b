# JAC IRC Proxy Configuration
# Copy this to .env and configure

# ===========================================
# REQUIRED: Your domain and email (for SSL)
# ===========================================

DOMAIN=irc.yourdomain.com
ACME_EMAIL=admin@yourdomain.com

# ===========================================
# ADMIN API (Required for web admin panel)
# ===========================================

# Generate a secure token: openssl rand -hex 32
ADMIN_TOKEN=your-secure-token-here
ADMIN_PORT=6680

# ===========================================
# PROXY SETTINGS
# ===========================================

HOST=0.0.0.0
PORT=6667

# SSL (certificates managed by certbot)
SSL_ENABLED=true
SSL_PORT=6697

# Logging: debug, info, warn, error
LOG_LEVEL=info

# Custom WebSocket gateway URL (default: JAC production)
# WS_URL=wss://hliytlezggzryetekpvo.supabase.co/functions/v1/irc-gateway
